<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Internet permission for WebView -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Notification permissions for Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Foreground service permissions for persistent notification keep-alive -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Boot completed permission to restart service on device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Exact alarm permission for reminder scheduling (Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".HabitPulseApplication"
        android:allowBackup="true"
        android:appCategory="productivity"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HabitPulse.Splash">
        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.HabitPulse.Splash"
            android:enableOnBackInvokedCallback="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WelcomeActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.HabitPulse.Splash"
            android:enableOnBackInvokedCallback="true"
            android:resizeableActivity="true"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.HabitPulse"
            android:enableOnBackInvokedCallback="true"
            android:resizeableActivity="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:theme="@style/Theme.HabitPulse"
            android:enableOnBackInvokedCallback="true"
            android:resizeableActivity="true"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name=".NewSettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:theme="@style/Theme.HabitPulse"
            android:enableOnBackInvokedCallback="true"
            android:resizeableActivity="true"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name=".NewSettingsAIActivity"
            android:exported="false"
            android:label="@string/settings_category_ai"
            android:theme="@style/Theme.HabitPulse"
            android:enableOnBackInvokedCallback="true"
            android:resizeableActivity="true"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name=".NewSettingsAIProviderActivity"
            android:exported="false"
            android:label="@string/settings_configure_provider"
            android:theme="@style/Theme.HabitPulse"
            android:enableOnBackInvokedCallback="true"
            android:resizeableActivity="true"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name=".NewSettingsNotificationsActivity"
            android:exported="false"
            android:label="@string/settings_notifications"
            android:theme="@style/Theme.HabitPulse"
            android:enableOnBackInvokedCallback="true"
            android:resizeableActivity="true"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name=".NewSettingsReminderActivity"
            android:exported="false"
            android:label="@string/reminder_settings_title"
            android:theme="@style/Theme.HabitPulse"
            android:enableOnBackInvokedCallback="true"
            android:resizeableActivity="true"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name=".NewSettingsTemplateActivity"
            android:exported="false"
            android:label="@string/notification_template_settings_title"
            android:theme="@style/Theme.HabitPulse"
            android:enableOnBackInvokedCallback="true"
            android:resizeableActivity="true"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name=".NewSettingsGeneralActivity"
            android:exported="false"
            android:label="@string/settings_category_general"
            android:theme="@style/Theme.HabitPulse"
            android:enableOnBackInvokedCallback="true"
            android:resizeableActivity="true"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name=".NewSettingsAboutActivity"
            android:exported="false"
            android:label="@string/settings_about"
            android:theme="@style/Theme.HabitPulse"
            android:enableOnBackInvokedCallback="true"
            android:resizeableActivity="true"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name=".NewSettingsDebugActivity"
            android:exported="false"
            android:label="@string/settings_debug_title"
            android:theme="@style/Theme.HabitPulse"
            android:enableOnBackInvokedCallback="true"
            android:resizeableActivity="true"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name=".AISettingsActivity"
            android:exported="false"
            android:label="@string/ai_settings_title"
            android:theme="@style/Theme.HabitPulse"
            android:enableOnBackInvokedCallback="true"
            android:resizeableActivity="true"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name=".WebViewActivity"
            android:exported="false"
            android:label="@string/webview_help"
            android:theme="@style/Theme.HabitPulse"
            android:enableOnBackInvokedCallback="true"
            android:resizeableActivity="true"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name=".OpenSourceLicensesActivity"
            android:exported="false"
            android:label="@string/settings_open_source_licenses"
            android:theme="@style/Theme.HabitPulse"
            android:enableOnBackInvokedCallback="true"
            android:resizeableActivity="true"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name=".ReminderSettingsActivity"
            android:exported="false"
            android:label="@string/reminder_settings_title"
            android:theme="@style/Theme.HabitPulse"
            android:enableOnBackInvokedCallback="true"
            android:resizeableActivity="true"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize" />

        <!-- Foreground notification service for keep-alive -->
        <!-- dataSync type: keep app alive for reliable reminder scheduling and habit data sync -->
        <service
            android:name=".service.ForegroundNotificationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Boot completed receiver to restart foreground service and reminders on device reboot -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Reminder alarm receiver for 30-minute habit notifications -->
        <receiver
            android:name=".receiver.ReminderReceiver"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>